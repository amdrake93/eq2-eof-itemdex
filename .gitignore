# Binaries
/bin/
*.exe
*.out
*.test

# Go
/vendor/
go.work
go.work.sum

# Env / OS
.env
.DS_Store

# Generated CSVs are intentionally tracked (shareable catalog + cache).
# Uncomment to treat them as local-only instead:
# /data/*.csv

# Resume-state file from interrupted multi-session pulls — never commit
/data/.census_next_offset
/bis.db

# Generated per-character outputs — local only (config.toml is committed)
/characters/*/loadout.toml
/characters/*/upgrade-report.md
/characters/*/bis-report.md
